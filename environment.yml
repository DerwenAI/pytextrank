name: pytextrank
channels:
  - conda-forge
  - defaults
dependencies:
  - python >= 3.6
  - pip
  - graphviz >= 0.13
  - altair >= 4.1
  - networkx >= 2.6
  - spacy >= 3.0
  - scipy >= 1.7
  - pip:
    - icecream >= 2.0
